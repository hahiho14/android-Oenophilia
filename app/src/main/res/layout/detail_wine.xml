<?xml version="1.0" encoding="utf-8"?>
<ScrollView xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent">
    <LinearLayout
        android:orientation="vertical"
        android:layout_width="match_parent"
        android:layout_height="match_parent">

        <FrameLayout
            android:layout_width="match_parent"
            android:layout_height="wrap_content">

            <ImageView
                android:id="@+id/iv_detail_wine"
                android:layout_width="match_parent"
                android:layout_height="250dp"
                android:adjustViewBounds="true"
                android:scaleType="fitCenter"
                tools:src="@android:color/darker_gray" />
        </FrameLayout>

        <TextView
            android:id="@+id/tv_detail_wine_name"
            style="@style/TextContent"
            android:layout_marginTop="@dimen/activity_vertical_margin"
            android:layout_marginBottom="8dp"
            android:text="@string/wine_name"
            android:textSize="24sp" />

        <TextView
            android:id="@+id/tv_detail_avg_price"
            style="@style/TextContent"
            android:layout_marginBottom="@dimen/activity_vertical_margin"
            android:text="@string/avg_price"/>

        <TextView
            android:id="@+id/tv_detail_wine_desc"
            style="@style/TextContent"
            android:layout_marginBottom="@dimen/activity_vertical_margin"
            android:lineSpacingMultiplier="1"
            android:text="@string/wine_desc"
            android:textColor="@android:color/black" />

    </LinearLayout>
</ScrollView>